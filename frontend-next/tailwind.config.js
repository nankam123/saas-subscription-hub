/** @type {import('tailwindcss').Config} */
module.exports = {
   content: [
    "./src/**/*.{js,ts,jsx,tsx}", // scan everything in src folder
  ],
  theme: {
    extend: {}, // use this to add custom colors, fonts, spacing, etc.
  },
  plugins: [],
}

